with tab2:
    st.header("Terminal de Execução")
    st.info("⚠️ Funcionalidade de negociação ativa apenas em execução local por restrições de IP no servidor cloud.")
    # Remova ou comente o bloco try/except que chama o ccxt aqui
    
